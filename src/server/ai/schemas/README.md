# AI Schemas

This directory will contain JSON schema definitions and Zod models used to validate AI extraction payloads.

Planned contents include:

- Typed schemas for different email types (invoice, meeting, support, etc.)
- Versioned schema definitions aligned with `schemaVersion` stored in MongoDB

