# AI Prompts

This directory will contain prompt templates and prompt-building utilities used for AI-powered email analysis.

At this stage, no prompts are implemented. Future work will add:

- Reusable system and user prompts for classification and extraction
- Versioned prompt definitions aligned with `promptVersion` stored in MongoDB

